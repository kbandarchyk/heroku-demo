build:
  docker:
    web: heroku-web/Dockerfile --build-arg profile=production
    worker: heroku-worker/Dockerfile --build-arg profile=production
run:
  web: java -jar target/heroku-web-1.0-SNAPSHOT.jar
  worker: java -jar target/heroku-worker-1.0-SNAPSHOT.jar
